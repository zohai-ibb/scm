#url
app.base-url=https://scm-production-045d.up.railway.app

# Railway MySQL database
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=LNepEBVdGdXTgyxIFoJcLBkZdFXMirNZ
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

# Multipart file upload
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

# Cloudinary
cloudinary.cloud.name=${CLOUDINARY_NAME:dy5xalshq}
cloudinary.api.key=${CLOUDINARY_API_KEY:919985897651359}
cloudinary.api.secret=${CLOUDINARY_API_SECRET:JT8u9eOOfpapbUf7L0CaI4Vkkvw}

# Email
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:johaibansari002@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xtsg foqj gfbf qdao}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS_ENABLE:true}
spring.mail.properties.domain_name=${MAIL_DOMAIN:scm@demomailtrap.co}

# OAuth2 - Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=${GOOGLE_SCOPE:email,profile}

# OAuth2 - GitHub
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=${GITHUB_SCOPE:read:user,user:email}

# Web3Forms
web3forms.access_key=${WEB3FORMS_ACCESS_KEY:a22ea432-4fb5-46e6-a0b9-931b8988aa54}
